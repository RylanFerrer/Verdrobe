export const time = () => {
    const date = new Date();
    const hour = date.getHours()
    if(hour > 1 && hour <= 11)
    {
        return "Good Morning"
    } else if (hour > 12 && hour  )
}